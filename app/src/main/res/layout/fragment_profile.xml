<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="user"
            type="com.ez.dotarate.model.User" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_marginTop="@dimen/toolbar_height">

        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/avatar"
            android:layout_width="@dimen/image_size"
            android:layout_height="@dimen/image_size"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="16dp"
            app:civ_border_color="@color/colorGreen"
            app:civ_border_width="2dp"
            app:errorImage="@{@drawable/ic_empty_avatar}"
            app:url="@{user.userResponse.userPlayers[0].avatarfull}"
            tools:src="@drawable/ic_empty_avatar">

        </de.hdodenhof.circleimageview.CircleImageView>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:text="@{user.userResponse.userPlayers.get(0).personaname}"
            android:textSize="22sp"
            tools:text="Белый Господин" />

    </LinearLayout>
</layout>